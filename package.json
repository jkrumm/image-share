{
  "name": "image-share",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "./scripts/dev.sh",
    "test": "bun run --cwd apps/api test && bun run --cwd apps/admin test",
    "typecheck": "tsc --noEmit -p apps/api && bun run --cwd apps/admin typecheck",
    "lint": "oxlint .",
    "lint:basalt": "bun run --cwd apps/admin lint:basalt",
    "lint:fix": "oxlint --fix .",
    "format": "oxfmt 'apps/**' '!**/dist/**' '!**/drizzle/**' --write",
    "format:check": "oxfmt 'apps/**' '!**/dist/**' '!**/drizzle/**' --check",
    "check": "bun run lint && bun run lint:basalt && bun run format:check && bun run typecheck && bun run test",
    "db:generate": "bun run --cwd apps/api db:generate"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "lefthook": "^2.1.14",
    "oxfmt": "^0.45.0",
    "oxlint": "1.74.0",
    "typescript": "^5.9.3"
  }
}
